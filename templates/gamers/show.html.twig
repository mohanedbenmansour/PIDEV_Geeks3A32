{% extends 'base.back.html.twig' %}

{% block title %}Gamers{% endblock %}

{% block body %}
    <h1>Gamers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gamer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gamer.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ gamer.age }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ gamer.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gamers_index') }}">back to list</a>

    <a href="{{ path('gamers_edit', {'id': gamer.id}) }}">edit</a>

    {{ include('gamers/_delete_form.html.twig') }}
{% endblock %}
